#include <stdio.h>

// Can interact with existing Frameworks here by linking against them (see Makefile) and declaring their exported symbols, e.g.
// extern void exported_func();

int main() {
    puts("Hello World from iOS!");
    return 0;
}
